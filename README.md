# Tower-Simulator-3-Optimizer
 Airport Optimizer is an unofficial Windows utility for Tower! Simulator 3. It detects verified airports, analyzes storage use, creates backups, and safely applies or removes NTFS compression without moving, renaming, unpacking, or modifying proprietary `.asset` contents.
